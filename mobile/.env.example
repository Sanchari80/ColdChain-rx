# Only needed if the app cannot work out where the server is on its own.
# On a physical phone this must be your computer's LAN address, not localhost.
# EXPO_PUBLIC_API_URL=http://192.168.0.101:4000
